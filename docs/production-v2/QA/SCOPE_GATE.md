# Per-scope quality gate

Before reporting a scope complete: reread its scope and locked invariants; search old paths meant to be replaced; run targeted tests then full verification required by scope; perform relevant desktop browser checks; inspect console; ensure no accidental provider calls; update PROGRESS; list new/reused/refactored/deleted components, compatibility retained and known issues. A known requirement inside the scope cannot be moved to “later” without founder approval.
