# Future seams — do not implement now

Naomi/Waveform Playlist is future advanced temporal editing: trim, split, fades, clip positioning, multi-track timing, richer music/SFX. Sequence remains canonical editorial view. Timing is replaceable secondary view. Naomi never owns Part, Take, Cast, Composer or global Player state.

Future SFX/effects may add Narration, Music, SFX and effect lanes. Do not invent domain now; Workbench/Timing geometry should accept another typed editor later.

CosyVoice is not activated. When validated it appears as additional exact routes/capabilities. If adding it requires provider checks scattered through product components, Production v2 failed.

No special Conversation mode now. Cast colors + alternating Speech Parts already represent dialogue; a future Dialogue view can be another representation if real usage proves need.
